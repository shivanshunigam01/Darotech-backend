import { Router } from 'express'; import * as c from '../controllers/auth.controller.js'; import { validate } from '../middlewares/validate.js'; import { loginSchema } from '../validators/auth.validator.js'; import { requireAuth } from '../middlewares/auth.js'; import { authLimiter } from '../middlewares/rateLimit.js'; const r=Router(); r.post('/login',authLimiter,validate(loginSchema),c.login); r.post('/refresh',c.refresh); r.post('/logout',c.logout); r.get('/me',requireAuth,c.me); export default r;
