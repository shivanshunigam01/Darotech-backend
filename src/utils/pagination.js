export async function paginate(Model,{page=1,limit=10,filter={},sort='-createdAt',populate=[]}={}){ page=Math.max(Number(page)||1,1); limit=Math.min(Math.max(Number(limit)||10,1),100); const q=Model.find(filter).sort(sort).skip((page-1)*limit).limit(limit); if(populate) q.populate(populate); const [items,total]=await Promise.all([q, Model.countDocuments(filter)]); return {items,total,page,pages:Math.ceil(total/limit)||1,limit}; }
