import PDFDocument from 'pdfkit';
export function invoicePdfStream(payment){ const doc=new PDFDocument({margin:50}); doc.fontSize(20).text('Darotech Technology - Invoice',{align:'center'}); doc.moveDown(); doc.fontSize(12).text(`Invoice No: ${payment.invoiceNo}`); doc.text(`Customer: ${payment.customerName||''}`); doc.text(`Email: ${payment.email||''}`); doc.text(`Service: ${payment.service||''}`); doc.text(`Package: ${payment.package||''}`); doc.text(`Amount: ${payment.currency||'INR'} ${payment.amount}`); doc.text(`Status: ${payment.status}`); doc.text(`Date: ${payment.paymentDate||payment.createdAt}`); doc.end(); return doc; }
