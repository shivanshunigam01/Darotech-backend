import app from './app.js'; import { connectDB } from './config/db.js'; import { env } from './config/env.js'; import { logger } from './config/logger.js'; import './jobs/leadFollowup.job.js'; const start=async()=>{await connectDB(); app.listen(env.PORT,()=>logger.info(`API ready → http://localhost:${env.PORT}${env.API_PREFIX}`));}; start().catch(e=>{logger.error(e); process.exit(1);});
