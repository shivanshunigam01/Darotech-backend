import { Schema, model } from 'mongoose'; export default model('Blog',new Schema({title:{type:String,required:true},slug:{type:String,required:true,unique:true,index:true},category:String,author:{type:Schema.Types.ObjectId,ref:'User'},featuredImage:String,shortDescription:String,content:String,tags:[String],seoTitle:String,seoDescription:String,status:{type:String,enum:['Published','Draft'],default:'Draft'},publishDate:Date},{timestamps:true}));
