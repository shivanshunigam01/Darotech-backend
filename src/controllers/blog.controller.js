import { makeCrud } from './crudFactory.js'; import Model from '../models/Blog.js'; export const { create, list, get, getBySlug, update, remove } = makeCrud(Model,{searchFields:['title', 'category']});
