import { z } from 'zod'; export const orderSchema=z.object({body:z.object({amount:z.number().positive(),customerName:z.string().min(2),email:z.string().email(),phone:z.string().optional(),service:z.string().optional(),pkg:z.string().optional(),gateway:z.enum(['Razorpay','Stripe','Manual']).optional()})}); export const verifySchema=z.object({body:z.object({razorpay_order_id:z.string(),razorpay_payment_id:z.string(),razorpay_signature:z.string()})});
